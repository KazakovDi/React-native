import React from 'react';
import {View} from 'react-native';
const SectionDevider = () => {
  return <View style={{height: 20, borderColor: '#000', borderWidth: 3}} />;
};

export default SectionDevider;
